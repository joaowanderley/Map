import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    height: 200,
    width: 300,
    position: 'absolute',
    bottom: 0,
    elevation: 3,
    
    alignItems: 'center',
  }
})

export default styles